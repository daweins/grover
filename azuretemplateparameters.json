{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "clientID": {
            "type": "string",
            "metadata": {
                "description": "The clientID of an existing Service Principal with ownership permissions on the subscription "
            }
        },
        "clientPassword": {
            "type": "string",
            "metadata": {
                "description": "The password of the existing Service Principal with ownership permissions on the subscription "
            }
        }
    }
}